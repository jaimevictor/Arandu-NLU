# Decisões abertas

## Estatuto

**CONVENÇÃO APROVADA.** Este registro contém todas as escolhas humanas ainda abertas identificadas no pacote, de `OPEN-001` a `OPEN-026`. A evidência citada é exclusivamente interna a este handoff. Opções conhecidas delimitam alternativas; não significam aprovação.

**DECISÃO PENDENTE.** Nenhuma linha abaixo possui resposta implícita. `OPEN-002`, `OPEN-003` e `OPEN-014` são bloqueios imediatos da futura fundação e devem ser decididas antes de qualquer implementação.

**CONVENÇÃO APROVADA.** Somente `OPEN-002`, `OPEN-003` e `OPEN-014` bloqueiam imediatamente a fundação. As demais decisões bloqueiam apenas a camada ou a porta indicada em sua fase limite; essa distinção não autoriza antecipar nenhuma delas.

## Registro

| ID | Impacto | Opções conhecidas | Evidência disponível | Recomendação se houver base | Menor pergunta necessária ao usuário | Fase limite |
| --- | --- | --- | --- | --- | --- | --- |
| OPEN-001 | **DECISÃO PENDENTE.** Define o catálogo mínimo de intenções, slots e casos Home Assistant da primeira versão; sem ele não há escopo semântico fechado nem oráculo de aceite do domínio. | Catálogo mínimo fechado; núcleo obrigatório com extensões versionadas; adiar capacidades que não possam ser especificadas com dados aprovados. | `FR-010` e `FR-011` exigem esquemas versionados; `FR-012` admite várias intenções; o pacote não enumera conceitos de domínio nem casos linguísticos. | Definir um catálogo pequeno, versionado e testável antes dos esquemas; não há base no pacote para escolher seu conteúdo. | Qual catálogo mínimo de intenções, slots e casos Home Assistant compõe a primeira versão? | Antes de criar esquemas semânticos ou casos de aceitação do domínio. |
| OPEN-002 | **DECISÃO PENDENTE — BLOQUEIO IMEDIATO DA FUTURA FUNDAÇÃO.** Determina quais transformações Unicode, de caixa e de diacríticos podem produzir a representação de trabalho sem destruir o original. | Preservação sem transformação; forma canônica escolhida com mapa; representações derivadas específicas por finalidade; combinação versionada dessas políticas. | `FR-003`, `FR-004`, `NFR-005` e `DATA-007` aprovam preservação, rastreabilidade e separação entre original e derivado, mas não escolhem a política. | Preservar sempre o original e versionar toda transformação já é obrigatório; não há base para escolher a forma canônica nem o tratamento de caixa e diacríticos. | Qual política normativa de normalização, caixa e diacríticos deve reger a representação de trabalho? | Antes de iniciar a futura fundação ou implementar qualquer transformação textual. |
| OPEN-003 | **DECISÃO PENDENTE — BLOQUEIO IMEDIATO DA FUTURA FUNDAÇÃO.** Define unidade, origem e intervalos dos offsets e o mapeamento entre texto original e derivado. | Bytes; unidades de código; pontos de código; agrupamentos visuais; mais de uma coordenada com conversão explícita. Intervalos podem ter limite final incluído ou excluído. | `FR-004`, `FR-005`, `NFR-005` e `TEST-004` exigem offsets válidos e rastreáveis, mas não aprovam unidade nem convenção. | Adotar uma coordenada canônica e conversões explícitas, mantendo vínculo com o original; o pacote não sustenta qual coordenada escolher. | Qual unidade, origem e convenção de intervalo serão canônicas, e qual mapeamento será obrigatório entre original e derivado? | Antes de iniciar a futura fundação ou definir Unidade Textual. |
| OPEN-004 | **DECISÃO PENDENTE.** Define a representação das análises linguísticas e o conjunto de rótulos necessários para que componentes substituíveis troquem informação sem acoplamento oculto. | Conjunto interno mínimo; conjunto externo aprovado; representação interna com mapeamentos versionados; nenhuma rotulação além da necessária à primeira versão. | `FR-006` a `FR-010` exigem alternativas e composição rastreável; `NFR-012` exige substituição por contrato; nenhuma taxonomia linguística está aprovada. | Minimizar rótulos e exigir definição, versão, proveniência e mapeamento; não há base para selecionar uma taxonomia. | Qual representação linguística e qual conjunto mínimo de rótulos serão normativos na primeira versão? | Antes de implementar a primeira camada que produza ou consuma rótulos linguísticos. |
| OPEN-005 | **DECISÃO PENDENTE.** Determina como contexto permitido elimina ou mantém análises concorrentes sem transformar preferência em certeza. | Manter alternativas até restrição posterior; aplicar desempate determinístico versionado; declarar grupos empatados; abster quando a evidência continuar insuficiente; combinar essas opções por estágio. | `FR-006`, `FR-009` e `FR-016` preservam alternativas e distinguem insuficiência de ambiguidade; `NFR-001` exige repetibilidade. | Toda decisão deve ser determinística e auditável, com empate explícito e abstinência quando faltar base; a estratégia concreta permanece sem evidência suficiente. | Qual estratégia contextual de desempate deve ser aplicada, e em qual estágio alternativas podem ser removidas? | Antes de implementar desambiguação contextual ou consolidar hipóteses. |
| OPEN-006 | **DECISÃO PENDENTE.** Decide se correção contextual integra a primeira versão e, se integrar, quando uma sugestão pode influenciar análises posteriores. | Não incluir na primeira versão; produzir apenas sugestões; permitir uso controlado da sugestão mantendo original e alternativas; habilitar por configuração versionada. | `FR-007` obriga que qualquer correção seja sugestão rastreável e preserve o original; o pacote não obriga ativar essa capacidade. | Se incluída, manter original, motivo e alternativas e impedir substituição silenciosa; não há base para decidir inclusão. | A correção contextual entra na primeira versão e, se entrar, qual influência uma sugestão pode exercer? | Antes de congelar o escopo ou aceitar a primeira versão e, em qualquer caso, antes de implementar ou expor a capacidade. |
| OPEN-007 | **DECISÃO PENDENTE.** Define representação de pontuação ou confiança, ordem das hipóteses, empates e significado consumível por clientes. | Sem pontuação, apenas relações e empates; ordem ordinal; medida limitada com semântica documentada; medida calibrada somente após avaliação aprovada. | `FR-006`, `FR-009`, `API-004` e `NFR-001` exigem pluralidade e ordem determinística; nenhum valor, escala ou interpretação probabilística foi aprovado. | Expor empates e uma ordem estável; não apresentar medida como probabilidade sem calibração verificável. Não há base para escolher escala. | As hipóteses terão somente ordem, terão pontuação, ou ambas, e qual é a semântica de empate? | Antes de congelar o resultado de hipóteses ou implementar seleção entre candidatas. |
| OPEN-008 | **DECISÃO PENDENTE.** Define conflitos e dependências entre múltiplas intenções e se partes independentes podem seguir para execução quando outra parte falhar ou permanecer pendente. | Atomicidade total; execução somente de itens independentes elegíveis; grupos atômicos explícitos; proibição inicial de execução parcial. | `FR-012` exige conflitos, dependências e limites explícitos; `API-010` representa parcialidade quando aplicável; a política de autorização não aprova execução parcial. | Bloquear sempre o item afetado e nunca inferir independência; falta base para escolher entre atomicidade global e parcialidade explicitamente autorizada. | Uma solicitação com várias intenções é atômica ou pode executar subconjuntos independentes, e como a independência é declarada? | Antes de habilitar propostas multi-intenção na fronteira de execução. |
| OPEN-009 | **DECISÃO PENDENTE.** Define identidade de entidades do Home Assistant, precedência de aliases, resolução de conflitos e validade dos snapshots do catálogo. | Identidade externa opaca; identidade interna com mapeamento; precedência explícita por classe de nome; conflito sempre ambíguo; validade por versão, tempo ou substituição explícita. | `FR-013` a `FR-015`, `API-008`, `SEC-006`, `SEC-008` e `DATA-013` exigem snapshot imutável, classes separadas e ambiguidade observável, sem escolher identidade ou precedência. | Usar identidade estável, snapshot imutável e conflitos explícitos; não há base para definir formato, precedência ou prazo de validade. | Qual contrato define identidade, precedência de aliases, conflito e validade de cada snapshot? | Antes de implementar catálogo dinâmico ou resolução de entidades. |
| OPEN-010 | **DECISÃO PENDENTE.** Define identidade, duração, armazenamento, expiração, reinicialização e concorrência das sessões. | Somente memória efêmera; persistência local; expiração por inatividade, duração absoluta, turnos ou combinação; controle otimista ou serialização de atualizações; reinicialização total ou seletiva. | `FR-017` a `FR-020`, `API-005` a `API-007` e `SEC-010` exigem contexto explícito, isolado e determinístico, mas não escolhem política de ciclo de vida. | Manter estado mínimo, local, versionado e isolado, com expiração e reinicialização explícitas; não há base para os parâmetros ou armazenamento. | Qual identidade, armazenamento, regra de expiração, reinicialização e concorrência deve reger a sessão? | Antes de implementar contexto entre turnos ou pedidos de esclarecimento persistentes. |
| OPEN-011 | **DECISÃO PENDENTE.** Define quais ações são sensíveis, quando confirmar, quem possui autoridade para confirmar e como vincular a confirmação à proposta exata. | Negar todas as ações sensíveis na primeira versão; confirmação explícita por cliente autorizado; política configurada por classe de ação; autoridade externa com prova vinculada e expiração. | `FR-021`, `SEC-009` e `SEC-014` separam proposta, autorização e efeito; nenhuma classificação de sensibilidade nem autoridade concreta está aprovada. | Negar por padrão quando classificação ou confirmação for ausente, inválida ou expirada; a taxonomia e a autoridade exigem decisão humana. | Quais ações são sensíveis e qual autoridade pode emitir uma confirmação válida para a proposta exata? | Antes de habilitar qualquer execução externa de ação potencialmente sensível. |
| OPEN-012 | **DECISÃO PENDENTE.** Define transporte local, serialização e autenticação entre processos, afetando isolamento, compatibilidade e superfície de ataque. | Canal local mediado pelo sistema operacional; fila local durável; memória compartilhada com sincronização; serialização textual ou binária versionada; identidade do par pelo sistema operacional ou por credencial própria da fronteira. | `API-001` a `API-013` definem semântica sem tecnologia; `SEC-001` exige processos separados para interpretação e execução; `NFR-002` e `NFR-003` exigem operação local e runtime sem rede. | Escolher somente depois de `OPEN-014`; preservar contratos tipados, autenticar a fronteira exigida e não introduzir dependência externa. Não há base para tecnologia ou formato específicos. | Qual transporte local, serialização e mecanismo de autenticação entre processos serão normativos? | Antes de implementar integração entre processos ou congelar o formato externo. |
| OPEN-013 | **DECISÃO PENDENTE.** Define compatibilidade de versões e negociação de capacidades entre contratos, artefatos e consumidores. | Igualdade estrita; intervalos de compatibilidade; descoberta de capacidades com aceite explícito; rejeição sem negociação; combinação por versão principal e capacidade. | `FR-024`, `API-012`, `API-013` e `NFR-006` exigem identificação e rejeição explícita de incompatibilidade, sem aprovar regra de evolução. | Proibir rebaixamento silencioso e mudança de significado; falta base para escolher entre negociação e igualdade estrita. | Qual regra de compatibilidade e negociação será aplicada a contratos, capacidades e artefatos? | Antes de publicar a primeira versão estável de contrato ou artefato. |
| OPEN-014 | **DECISÃO PENDENTE — BLOQUEIO IMEDIATO DA FUTURA FUNDAÇÃO.** Define plataformas suportadas, condicionando ferramentas, empacotamento, isolamento, transporte, armazenamento e matriz de testes. | Uma plataforma inicial aprovada; conjunto limitado de plataformas; abstração portável com apenas uma plataforma inicialmente certificada. Nenhuma plataforma específica está aprovada. | `NFR-011` proíbe alegar suporte fora da matriz aprovada; decisões de transporte, isolamento e testes dependem do ambiente escolhido. | Aprovar ao menos uma plataforma e sua combinação mínima de arquitetura e ambiente antes da fundação; o pacote não contém base para escolher qual. | Qual plataforma, arquitetura e ambiente mínimos serão oficialmente suportados na primeira versão? | Antes de iniciar a futura fundação, escolher ferramentas ou criar empacotamento. |
| OPEN-015 | **DECISÃO PENDENTE.** Define limites de entrada, sessão, alternativas, candidatos, memória e tempo e o comportamento seguro ao excedê-los. | Limites fixos versionados; limites configuráveis com máximos; orçamentos por estágio; rejeição integral, abstinência localizada ou encerramento controlado ao exceder. | `FR-001`, `FR-012`, `SEC-012`, `NFR-009` e `TEST-016` exigem falha controlada e medição, mas nenhum valor ou política de sobrecarga foi aprovado. | Medir antes de fixar valores, nunca truncar de modo semanticamente invisível e rejeitar com categoria estável quando necessário. | Quais orçamentos máximos e qual comportamento de excesso devem valer em cada fronteira? | Antes de expor contratos a clientes não confiáveis ou realizar testes de carga para liberação. |
| OPEN-016 | **DECISÃO PENDENTE.** Define quais fontes linguísticas podem ser selecionadas, sua compatibilidade de licença e o nível de revisão humana exigido. | Fontes públicas com uso compatível; dados próprios documentados; fontes combinadas mantidas separadamente; rejeição de qualquer fonte com origem ou licença inconclusiva. | `FR-025`, `DATA-002`, `DATA-004` e `DATA-016` exigem proveniência, licença, hash e revisão, mas não selecionam fonte alguma. | Avaliar cada fonte individualmente e bloquear dúvida; não há base neste pacote para nomear ou aprovar fontes. | Quais fontes candidatas devem ser submetidas à revisão de origem, licença e adequação? | Antes de baixar, importar, transformar ou compilar qualquer dado linguístico. |
| OPEN-017 | **DECISÃO PENDENTE.** Define particionamento, custódia e acesso à avaliação oculta, afetando independência das métricas e prevenção de vazamento. | Custódia por papel humano separado; armazenamento isolado com acesso controlado; execução cega por serviço local controlado; partição reproduzível por grupos de origem e derivados. | `DATA-010` a `DATA-012` e `TEST-017` exigem separação, linhagem e ausência de acesso pelo desenvolvimento; proporção e mecanismo de custódia não estão aprovados. | Separar física e logicamente, atribuir custódia fora do fluxo de desenvolvimento e particionar por origem e derivados; parâmetros concretos exigem decisão. | Quem custodiará a avaliação oculta e qual regra reproduzível criará e protegerá suas partições? | Antes de criar, particionar ou acessar qualquer conjunto de treino, validação ou avaliação. |
| OPEN-018 | **DECISÃO PENDENTE.** Define metas de desempenho e regressão depois que existir baseline reproduzível; até lá, nenhum número é critério de aceite. | Orçamentos absolutos por ambiente; limites relativos ao baseline; perfis por plataforma; combinação de orçamento e limiar de regressão. | `NFR-009` e `TEST-016` definem método e métricas, mas não fornecem baseline nem alvo aprovado. | Executar e revisar primeiro o baseline; então aprovar metas por plataforma e carga, com método e tolerância registrados. | Após o baseline, quais limites de desempenho e regressão serão critérios de liberação em cada perfil aprovado? | Depois do primeiro baseline válido e antes de usar desempenho como porta de liberação ou iniciar otimização dirigida a metas. |
| OPEN-019 | **DECISÃO PENDENTE.** Define retenção, armazenamento e acesso a logs e trilhas, equilibrando auditoria, minimização e resposta operacional. | Sem persistência por padrão; retenção local curta; armazenamento local protegido e limitado; exportação controlada fora do runtime; níveis distintos por classe de evento. | `FR-026`, `NFR-004`, `NFR-007`, `NFR-010` e `SEC-011` exigem auditoria e minimização, mas não aprovam prazo, meio ou papéis de acesso. | Coletar o mínimo, negar acesso por padrão, nunca registrar credenciais e manter texto e identificadores sensíveis ausentes por padrão; parâmetros concretos seguem pendentes. | Qual prazo de retenção, local de armazenamento e matriz de acesso devem valer para cada classe de evento? | Antes de conservar observabilidade fora de testes controlados ou operar com dados de usuários. |
| OPEN-020 | **DECISÃO PENDENTE.** Define conteúdo, governança, versionamento e localização dos modelos de resposta textual. | Catálogo mínimo fixo e versionado; recursos localizados por variante; realização pelo cliente a partir de classe tipada; combinação com fallback textual mínimo aprovado. | `FR-023` e `API-011` exigem resposta textual determinística e fiel à situação, sem definir conteúdo, formato nem processo de revisão. | Separar resposta semântica de realização, versionar recursos e exigir revisão humana e governança de dados; não há base para redigir ou escolher conteúdo nesta fase. | Quem aprova o conteúdo e quais variantes, classes de resposta e regras de fallback entram no primeiro catálogo? | Antes de criar conteúdo de resposta para produção ou prometer localização ao usuário. |
| OPEN-021 | **DECISÃO PENDENTE.** Define identidade, autenticação e autorização dos clientes e processos que acessam contratos, sessões, catálogos, observabilidade e a fronteira de execução. Sem essa decisão, correlação técnica não prova quem chama nem quais capacidades pode usar. | Identidade fornecida pelo sistema operacional; registro local de clientes permitidos; credencial própria por cliente ou processo; capacidades ou papéis explícitos; combinação versionada com autenticação mútua na fronteira. | `API-001`, `API-007`, `API-012`, `SEC-001`, `SEC-002`, `SEC-010` e `SEC-012` exigem correlação, isolamento e validação, mas não definem identidade ou autorização do chamador. `OPEN-012` decidirá o transporte. | Negar por padrão, usar identidade estável e verificável, conceder somente capacidades mínimas e não tratar conhecimento de um identificador de sessão como autorização. A tecnologia depende de `OPEN-012` e `OPEN-014`. | Quais classes de cliente e processo existirão, como cada uma provará identidade e quais capacidades poderá exercer? | Antes de expor qualquer contrato a cliente não confiável ou habilitar comunicação entre processos fora de teste isolado. |
| OPEN-022 | **DECISÃO PENDENTE.** Define o ciclo completo das credenciais usadas pelo adaptador Home Assistant: provisionamento, armazenamento, acesso, rotação, revogação, expiração e recuperação após comprometimento. | Armazenamento protegido pelo sistema operacional; arquivo local cifrado com chave separada; agente local de segredos; provisionamento manual controlado; credenciais de curta duração quando suportadas; combinação por plataforma. | `FR-021`, `SEC-002`, `SEC-003`, `SEC-011` e `TEST-010` confinam credenciais ao adaptador e proíbem vazamento, mas não aprovam como administrá-las. `OPEN-014` e `OPEN-021` afetam os mecanismos disponíveis e a autoridade operacional. | Nunca entregar credenciais ao núcleo, nunca registrá-las, limitar acesso ao processo adaptador e exigir rotação e revogação testáveis. Não há base para escolher armazenamento ou prazo. | Como as credenciais do adaptador serão provisionadas, protegidas, rotacionadas e revogadas em cada plataforma aprovada? | Antes de armazenar ou usar qualquer credencial real do Home Assistant. |
| OPEN-023 | **DECISÃO PENDENTE.** Define idempotência, repetição, deduplicação de propostas, reconciliação após resultado indeterminado, compensação e semântica transacional para uma ou várias operações. Uma política errada pode duplicar efeitos ou comunicar estado falso. | Nenhuma repetição automática; chave idempotente por proposta ou operação; repetição limitada a falhas comprovadamente anteriores ao efeito; consulta ou reconciliação explícita; grupos atômicos quando suportados; resultados parciais com compensação declarada. | `FR-012`, `FR-022`, `API-009`, `API-010`, `SEC-013`, `TEST-011` e `OPEN-008` exigem correlação, parcialidade explícita e ausência de sucesso fabricado, mas não escolhem semântica de retry ou transação. | Não repetir automaticamente após resultado indeterminado; preservar identidade e correlação da proposta; representar parcialidade; só compensar quando houver operação e política aprovadas. | Qual política de idempotência, retry, reconciliação, compensação e atomicidade vale para cada classe de operação? | Antes de executar ações reais, permitir retries ou habilitar propostas com múltiplas operações. |
| OPEN-024 | **DECISÃO PENDENTE.** Define a taxonomia concreta de erros, o formato e ciclo dos identificadores de correlação e o nível de detalhe seguro exposto a cada classe de consumidor. Afeta interoperabilidade, diagnóstico e privacidade. | Categorias estáveis com subcódigos versionados; códigos apenas por estágio; identificadores opacos por solicitação, turno ou operação; níveis de divulgação por papel; detalhe interno somente em observabilidade protegida; forma mínima segura comum. | `FR-001`, `FR-016`, `FR-022`, `FR-026`, `API-013`, `NFR-004`, `NFR-007` e `SEC-011` exigem erros distinguíveis, correlação e sanitização, sem aprovar códigos, formato ou visibilidade. | Começar com taxonomia mínima e estável, separar erro de abstinência e resultado de domínio, usar correlação opaca e expor somente o necessário à ação segura do consumidor. | Quais categorias, subcódigos, identificadores de correlação e níveis de detalhe serão públicos para cada classe de cliente? | Antes de congelar contratos externos, schemas de observabilidade ou comportamento de retry do cliente. |
| OPEN-025 | **DECISÃO PENDENTE.** Define a regra ou algoritmo de deduplicação por classe de dados, incluindo equivalência, agrupamento por origem e derivados, tratamento de conflitos e preservação de todas as proveniências. | Igualdade de hash bruto; igualdade de forma normalizada aprovada; chave composta específica por classe; agrupamento por linhagem; revisão humana para equivalências não exatas; nenhuma deduplicação automática em certas classes. | `DATA-001`, `DATA-008`, `DATA-009`, `DATA-013`, `TEST-013` e `TEST-017` proíbem fusão silenciosa e entre classes, mas não escolhem equivalência nem algoritmo. | Definir regra separada por classe, preservar todas as origens e licenças, não cruzar perfis ou classes e encaminhar conflito não exato para revisão humana. | Qual chave, equivalência e procedimento de conflito devem governar a deduplicação em cada classe de dados? | Antes de implementar deduplicação, fusão ou agrupamento no pipeline offline ou em armazenamento operacional. |
| OPEN-026 | **DECISÃO PENDENTE.** Define metas quantitativas de qualidade e cobertura funcional depois que houver baseline próprio, conjunto aprovado e catálogo de escopo fechado. Até lá, nenhum número pode ser porta de liberação. | Metas por intenção, slot ou capacidade; limites por classes de erro e abstinência; cobertura por requisito ou caso; pisos por estrato aprovado; limites relativos ao baseline; combinação com portas de segurança sem média agregada única. | `FR-009` a `FR-016`, `TEST-001`, `TEST-005` a `TEST-008`, a seção de métricas de `09-acceptance-test-plan.md`, `OPEN-001` e `OPEN-017` exigem medição e conjuntos controlados, mas não fornecem baseline nem alvo. | Fechar primeiro escopo e critérios de rotulação, aprovar partições e executar baseline reproduzível; então definir metas por capacidade e classe de falha sem ocultar abstinência ou violações de segurança em uma média. | Após baseline aprovado, quais metas de qualidade, abstinência, erro e cobertura serão critérios de liberação por capacidade? | Depois do primeiro baseline válido e antes de usar qualidade ou cobertura como porta quantitativa de liberação. |

## Controle de completude

### CONVENÇÃO APROVADA

- O registro contém exatamente `OPEN-001..OPEN-026`, sem lacunas de numeração.
- `OPEN-002`, `OPEN-003` e `OPEN-014` são os únicos bloqueios imediatos da futura fundação.
- `OPEN-001`, `OPEN-004..OPEN-013` e `OPEN-015..OPEN-026` devem ser resolvidos até a fase limite de cada linha.
- Uma nova decisão pendente deve receber novo ID estável, impacto, opções, evidência interna, recomendação quando houver base, pergunta mínima e fase limite; não pode permanecer escondida em prosa.

## Bloqueio da futura fundação

**DECISÃO PENDENTE.** A futura fundação não pode começar enquanto `OPEN-002`, `OPEN-003` e `OPEN-014` permanecerem sem resposta registrada. As demais decisões devem ser resolvidas até a fase limite indicada, sem serem convertidas em padrões implícitos.
