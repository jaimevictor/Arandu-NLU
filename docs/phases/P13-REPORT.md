# P13 Policy, Protocol, Server, And Transport Report

- Phase: `P13`
- State: `COMPLETE_BY_USER_DIRECTED_TRANSITION`
- Subject: `371ba75966c435e4d2026a24e427b8f35da79115`
- Subject tree: `6291bd5d349c79b9ac62d5b4ab324ad29628b61f`
- Result: `USER_DIRECTED_MINIMUM_CHECKPOINT`

## Delivered

P13 implements deny-first policy evaluation, protocol v2, a bounded
credential-free local server, and exact capability evidence for the selected
Noise revision 34 and Snow 0.10.0 transport portfolio. The transport remains
outside product runtime until P14 integrates it across the two-part Home
Assistant adapter boundary.

The final correction restores three retained Rust-source origin witnesses,
uses statement-local origin rejection, rejects decorated malformed decision
rows and embedded phase tokens, binds lifecycle states, and verifies existing
extracted source trees under the archive entry, path-depth, and byte limits.
It changes no product behavior, selected source byte, dependency identity,
protocol, cryptographic behavior, or linguistic input.

## Validation And Transition

Focused origin, governance, syntax, diff, and complete source-fetch regression
checks passed. The preceding subject's mandatory reviews found no product,
protocol, dependency, cryptographic, or linguistic defect and reproduced only
the bounded tooling and evidence defects corrected by this subject.

On 2026-08-31 the user explicitly directed the executor to stop P13 patch and
review work and move to P14 immediately. Therefore the expensive complete
source-inventory replay, self-binding refresh, aggregate P13 gate, and
same-subject reviewer set were not rerun on this checkpoint.

## P14 Carry-Forward

Before product transport admission, P14 must independently revalidate the
exact selected source from a kernel-enforced read-only Linux snapshot, prove
native amd64 and aarch64 reachability, and close any stale P13 aggregate
evidence or self-binding value encountered by that integration gate. This is
validation debt only; no known product defect remains open.
