# P13 Source Convergence Chronology

- Rejected portfolio: CPython 3.14.6 `ssl` plus OpenSSL 3.6.3 external TLS 1.3 PSK
- Replacement portfolio: Noise revision 34 plus Snow 0.10.0
- Maximum source convergence passes: 3
- Passes consumed: 3
- Additional candidate authority: `USR-019` through `USR-039`, each limited to
  the blocker set reproduced by the preceding frozen subject
- Current result: `USR_039_BLOCKER_CORRECTION_IN_PROGRESS`

## Chronology

1. Pass 1 established the exact source and probe tuple. Independent review
   reproduced OpenSSL's compiled CC0 SipHash component, incomplete material
   prohibition enforcement, and an unrecorded OpenSSL extraction-mode
   transform.
2. Pass 2 rejected the complete OpenSSL bundle, locked both CPython and
   OpenSSL material prohibitions, and recorded the OpenSSL transform.
   Quality review rejected the stale pass label.
3. Pass 3 corrected the pass ledger and the omitted Home Assistant and Wyoming
   mode transformations. P13 remained open and never reached a phase
   checkpoint. Its final closeout audit reproduced two additional compiled
   witnesses: CPython BLAKE2's CC0 dedication and OpenSSL AES's public-domain
   dedication. The audit also found that the conditional disposition could
   invite an unbounded sanitization effort.

No fourth CPython/OpenSSL convergence or sanitization pass occurred. ADR-0021
permanently rejects the archives, controlled build, Homebrew runtime, and every
projection derived from that portfolio.

The `USR-038` replacement froze as
`95d0eda79737d1be180f69f7560d51e619f84758`. Mandatory source and phase
review reproduced only five bounded tooling and evidence blocker classes:
three retained origin statements were omitted; same-line technical prose
could suppress a genuine origin; decorated malformed decision rows evaded
both parsers; lifecycle phase identity and passed/blocked fields were
insufficiently constrained; and existing-tree replay could inspect unexpected
or over-depth content before applying admission bounds. No review found a
product, protocol, dependency, cryptographic, transport-source, or linguistic
defect.

`USR-039` authorizes one final minimum correction of exactly those findings.
It restores the three source witnesses, makes rejection statement-local,
rejects decorated malformed rows and embedded phase tokens, binds every
lifecycle state, and verifies existing extracted trees under the archive
limits before reading content. The first mandatory same-subject PASS
checkpoints P13 immediately and advances to P14 without optional refinement.

`USR-019` authorized one consolidated correction. It selected the separately
sourced Noise/Snow portfolio in ADR-0022 and froze `2b687e5`. Independent
source review then reproduced four blocker classes:

1. RustSec's bundled file named as CC-BY-4.0 contained CC-BY-SA-4.0 and the
   root license hash was absent.
2. Fiat Crypto's `AUTHORS`, `CONTRIBUTORS`, and `COPYRIGHT` files were absent
   from the notice inventory and verifier predicate.
3. Cargo ancestor configuration could override the frozen backend while the
   gate still passed.
4. The Cacophony evidence checked blobs but not their exact recorded
   commit/tree/path bindings.

The newer completion instruction is recorded as `USR-020` and permits one
terminal source-evidence closure candidate. That candidate binds the official
CC-BY-4.0 legal code, records all Fiat notices, builds from private validated
source copies with a fresh Cargo home and exact effective/compiled graphs, and
checks Cacophony's exact Git objects. Fourteen project-authored compile-fail
`FIXTURE_TECNICA` entries satisfy disabled Cargo metadata lookups but are
prohibited from every selected or compiled graph.

This correction changes no product crate, schema, runtime dependency, or
linguistic input. The earlier `source-*.md` reports remain historical until
the terminal subject replaces them with same-baseline independent reports.
On PASS the subject checkpoints immediately; any P0-P2 failure records P13
blocked without another correction.

The `USR-020` subject `c3592db753814c36640a4e8a9b43492e6843bcfc`
then received five independent source reviews and one correctness review.
They converged on three evidence-only blocker classes: the material ledger
retained the parent lock identity, the new Creative Commons legal-code record
claimed admission before review completion, and private source bytes were not
rechecked after each Cargo command. No review found a product-code,
dependency-resolution, selected-source, license-eligibility, or transport
behavior defect.

The newer completion instruction is recorded as `USR-021` and permits one
consolidated correction limited to those three classes. It changes no product,
dependency, or external-source byte and adds no optional refinement. The
replacement receives one same-baseline source review set and, on PASS,
advances directly through the P13 final checkpoint.

The `USR-021` correction fixed those three classes. A subsequent independent
rights audit of the selected source reproduced two remaining blocker classes:
seven resolution-only crates still enlarged the admitted external-source
surface despite being absent from every selected graph, and selected Poly1305
and typenum bytes contained origin notices whose complete rights chains were
not yet bound by exact evidence.

`USR-022` permits exactly one rights-closure candidate. It replaces those
seven resolution-only crates with existing project-authored Apache-2.0
compile-fail fixtures, increasing the technical-fixture inventory from 14 to
22 while reducing selected external packages from 30 to 23. It projects
Poly1305 to the exact six retained files selected by
`poly1305_force_soft`, binds typenum's copied `num::pow` implementation to the
immutable rust-num MIT/Apache-2.0 origin, and enforces an exact 30-notice
inventory across all selected source. It changes no product crate or runtime
behavior. The first fully passing frozen baseline checkpoints immediately;
no optional rights-refinement pass is authorized.

The `USR-022` subject retained unrelated noncompiled files and an unresolved
Poly1305 Donna chain. `USR-023` therefore authorized one compile-only
projection and project-authored forced-soft backend correction. Its source
reviews then reproduced incomplete wrapped-origin detection and inconsistent
Rust notice and Poly1305 license-election evidence.

`USR-024` authorized the evidence-only correction frozen as `ec7e99c`. Two of
five source reviewers passed it. The remaining reviews reproduced one concrete
omitted Curve25519 `extracted from` statement, missing clean-acquisition
replay, one unguarded Git lazy-fetch path, transient Cargo source/config
substitution between endpoint checks, the stale 21-fixture description, and a
Linux file-union proof overclaim. No review found a product, protocol,
dependency-resolution, selected cryptographic source, or transport-behavior
defect.

`USR-025` permits one consolidated closeout correction for those exact
findings. It binds the concrete origin and acquisition recipe, makes all Git
reads fail without local objects, detects transient private-tree identity
changes, removes mutable Cargo configuration discovery, and limits P13's
Linux claim to exact source admission plus package graphs. Native Linux
read-only-snapshot compilation remains mandatory in P14. The first passing
same-baseline review set checkpoints immediately; no optional P13 refinement
is authorized.

The `USR-025` correction froze as
`2870a1bf806f9e2b046af22f2c286a400ee8910d`. Its five independent source
reviews converged on four remaining blocker classes: the selected Cargo binary
statically retained bytes from the permanently rejected OpenSSL portfolio;
the bounded origin vocabulary omitted concrete statements in retained
ChaCha20, Curve25519-dalek, Snow, and Zeroize files; acquisition lacked
deterministic directory/extraction replay and committed fetcher tests; and
private identity omitted the directory containing the source root, permitting
rename/substitute/restore.

`USR-026` permits one minimum integrated correction of only those classes.
P13 replaces Cargo execution with a fixed direct `rustc` and `clippy-driver`
plan, completes deterministic acquisition replay and tests, binds the concrete
origin rows, and includes the containing directory in command-window identity.
The selected profile, package identities, projected cryptographic source,
probe behavior, product code, and linguistic inputs do not change. The first
passing frozen replacement checkpoints immediately and advances to P14; no
optional review or refinement pass follows.

The `USR-026` correction froze as
`4c75002728e4c84b28d304dc8fc3707446b16262`. Mandatory review reproduced six
bounded validation defects: two test paths invoked unadmitted `true` and
`false` executables; two probe fields still described Cargo; the private-tree
identity began below a mutable ancestor; the aggregate review command could
still execute rejected Cargo; `librustc_driver` was omitted from the runtime
library identity; and command windows did not bind preexisting generated
artifacts.

`USR-027` permits one consolidated correction of only those defects. It uses
the admitted Ruby runtime for success and failure fixtures, requires explicit
no-Cargo P13 invocation, binds every lexical and resolved ancestor, binds all
five direct executables and runtime libraries, and snapshots preexisting
generated artifacts around every command. It corrects the probe evidence to
direct-rustc and direct-Clippy semantics and adds focused mutation tests. No
selected source, package identity, product byte, protocol, transport behavior,
or linguistic input changes. The first passing frozen replacement advances
immediately to P14.

Review of replacement `56c66e5303a329ae7f19e485d05f179bdcb8c59a`
then reproduced only first-creation/inter-command output identity, active
sysroot identity, inherited Cargo routing, two primary Git-origin checks, and
two notice-closure defects. `USR-028` permits one final blocker-only
correction. It binds twice-reproduced generated outputs and the complete target
sysroot, makes inherited no-Cargo paths Cargo-free, requires Noise and
Cacophony primary objects, and closes the Poly1305 and toolchain notices. This
is the maximum pass; the first passing frozen subject checkpoints with no
optional refinement.

The first `USR-028` implementation froze as
`971a440c0c4bdf1ba202f8569ea26dcd56cf77d8`. Five independent source
reviews all passed the active source, runtime, output, sysroot,
Cargo-exclusion, and notice controls, but two reproduced bounded evidence
defects: the
Noise material row omitted the source ledger's required tree and lacked a
field-for-field tuple relation, while four unused `exact_local_*` probe fields
pointed to obsolete pre-projection directories. One aggregate replay failed
closed because concurrent reviewers changed a shared ancestor; isolated
executor, discovery, license, provenance, and adversarial aggregate runs
passed, so no false acceptance or candidate defect was reproduced from that
event.

The consolidated correction adds the one missing material field and relation,
removes the four inactive local-path claims, and adds both mutation
regressions. It changes no selected archive, projected source, tool, generated
artifact, dependency, product behavior, protocol, or linguistic input. This
completes the already-authorized primary-origin evidence requirement rather
than opening another source-selection or optional refinement pass.

The Rust source-license correction under `USR-029` froze as
`0c29e26914a022a10a9bca717cd17fa83d018a9d`. Two of five independent source
reviews passed. Discovery, provenance, and defensive review reproduced three
bounded evidence defects: one stale zero-count contradicted the installed
unselected rust-analyzer helper; the toolchain provenance relation did not
read its recorded LLVM license; and a basename-prefix predicate omitted five
nested or nonstandard legal files. The omitted curl directory included
`BSD-4-Clause-UC` and `curl` texts that SPDX marks non-OSI.

The current correction removes the contradiction, binds the LLVM expression,
inventories the five files, and proves that their three containing packages
are outside the selected rustc/Clippy graph and sysroot. It adds exact
non-OSI rejection metadata and a coherent nested-BUSL mutation. No tool,
product, protocol, dependency, cryptographic, or linguistic byte changes. The
replacement receives the mandatory reviews once and checkpoints immediately
on PASS; this is blocker correction, not optional refinement.

That correction froze as
`41ea528df9b6163a9a08a297d21c9c8f8c18b386`. Five independent source
reviewers and the correctness reviewer reproduced five bounded blocker
classes:

1. four checksum-bound legal and attribution files named `THIRD_PARTY.txt`,
   `CREDITS.TXT`, `PACKAGERS`, and `THANKS` remained outside the inventory;
2. a coherent nested `LICENSE-BUSL` addition could enter the nominal legacy
   set because its recorded count was not reconciled with derived rows;
3. the Rust archive path could be substituted between its one-time hash and
   later `bsdtar` processes;
4. duplicate top-level toolchain source, manifest, verification, and archive
   fields were not bound to the authoritative records; and
5. a queued server request could time out at the socket and later enter a
   mutable request handler.

The exact failed reports are retained under
`docs/reviews/P13/history/41ea528d-*.md`.

`USR-030` records the newer instruction to finish P13 through one minimum
blocker-only correction. It expands the immutable legal inventory to 990
files, derives and hashes both the 981-file legacy partition and nine-file
reviewed delta, keeps one verified archive descriptor through every
extraction, binds every duplicated source identity, and gives queued requests
an atomic deadline-aware admission token. This correction adds regression
tests for each reproduced counterexample and no optional source, product, or
linguistic refinement. The first same-baseline PASS checkpoints P13
immediately and advances to P14.

The `USR-030` replacement froze as
`d64f2c65cb485a897bbf214affeec74907c212b8`. Mandatory reviews then
reproduced bounded same-inode archive rewrite, conventional legal-name,
post-admission timeout mutation, detached shutdown-worker, and missing
decision-ledger defects. `USR-031` authorized one correction using an
unlinked verified archive snapshot, complete legal aliases, serialized
admission expiration, aggregate worker joins, and restored governance
evidence. It changed no selected source or product semantics and authorized
no optional review or refinement.

That correction froze as
`06157fd4d42fcbad06c1418eb27cfb1514d81931`. Review reproduced three P2
classes: verified crate bytes were reopened by path, destination publication
could cross a replaced ancestor, and arbitrary admitted handlers could remain
live after a deadline. `USR-032` and ADR-0029 authorized exact verified-byte
consumption, descriptor-held publication, and dedicated-process fatal
containment. The correction again changed no source portfolio, dependency,
protocol, cryptographic, or linguistic byte.

The resulting subject `30329e2524dd86e0e30933cc9d780c341f95858e` froze next.
Source-license review passed; other mandatory source reviews reproduced archive
reopening, Git lazy-fetch, terminal-name substitution, cleanup races,
unbounded derived archive work, release panic abort, and a scheduler-independent
deadline overclaim. `USR-033` and ADR-0030 authorize one minimum correction.
The current correction consumes verified bytes, disables lazy fetch, performs
terminal name checks, atomically quarantines and retains uncertain artifacts,
bounds archive derivation, unwinds release panic, and retains server sockets
in fresh process-generation directories under a scheduling-conditional
contract. No selected source, dependency identity, protocol, cryptographic,
or linguistic byte changes. The first mandatory same-subject PASS checkpoints
immediately and advances directly to P14.

That correction froze as
`e04abd50fbd8f598068d975914e4a59787e71839`. Mandatory review reproduced
seven bounded blocker classes: closeout Git calls omitted the lazy-fetch guard;
an existing exact extraction's terminal name could change after its final
parent check; the exact archive path-depth boundary lacked a regression; an
expired active request could become invisible to the fatal monitor; concurrent
requests could dispatch increasing clock samples out of order; registry legal
discovery depended on filenames; and selected Rust toolchain origin statements
outside legal files lacked dispositions.

`USR-034` and ADR-0031 authorize only those corrections and exact regressions.
The replacement changes no selected source, dependency identity, protocol,
cryptographic byte, product behavior, or linguistic input. One immutable
subject receives the mandatory source and phase reviews. Its first complete
PASS checkpoints immediately and advances directly to P14, with no optional
review or refinement.

That correction froze as
`6a1851851091e6f3f0efd7e015331a15bfe86277`. Mandatory review reproduced
three bounded source-evidence classes: selected Rust archive metadata was
bounded only after extraction, selected compiler/Clippy/sysroot path sources
were not conservatively scanned, and two copied-source statements lacked
exact rights chains. `USR-035` and ADR-0032 authorized one correction adding
pre-extraction archive bounds, path-source observation, and exact Redwood and
Hyperium rights evidence.

The `USR-035` subject froze as
`a4a4f904b68447abcc6d24f00fc87b6aba8634f3`. Independent audit reproduced
only six bounded defects: a FIFO could block before type rejection; the
origin scanner omitted `based on`, `inspired by`, and non-first-line
`ported from`; a Unicode escape falsely matched CC0; Intel `cpuid.def`
contained restrictive unidentified terms; two LoongArch headers required
`GPL-3.0-or-later WITH GCC-exception-3.1`; and `USR-019` onward remained
absent from traceability and the manifest.

`USR-036` and ADR-0033 authorize the minimum correction of those exact
defects. It changes no selected external source byte, dependency identity,
protocol, cryptographic behavior, product behavior, or linguistic input. One
immutable subject receives the mandatory source and phase reviews. Its first
complete PASS checkpoints immediately and advances directly to P14, with no
optional review or refinement.

The `USR-036` correction froze as
`89cb007efc15a67b3f2c9af1af8973a7026538fa`. Five independent source
reviewers reproduced five bounded P2 classes: a globally valid origin tuple
could override a restrictive or nonstandard witness; bare, wrapped, and
non-first-line current origin statements were omitted; archive listing was
bounded only after complete buffering; and valid decision rows with harmless
delimiter whitespace could evade both governance parsers while a fixed
`USR-036` ceiling rejected future contiguous rows.

`USR-037` and ADR-0034 authorize one consolidated correction of those exact
defects. It changes no selected external source byte, dependency identity,
protocol, cryptographic behavior, product behavior, or linguistic input. The
first complete same-subject PASS checkpoints immediately and advances
directly to P14, with no optional review or refinement.

The `USR-037` correction froze as
`edf6e2c888c841c423f42a19c110072f3b6f7ec7`. Source-license review passed;
the remaining mandatory reviews reproduced bounded decision-row,
origin-discovery, rejection-scope, vocabulary-digest, extraction-output,
descendant-containment, YAML-amplification, and post-P00 lifecycle defects.
The findings changed no selected source byte, dependency, protocol,
cryptographic behavior, product behavior, or linguistic input.

`USR-038` and ADR-0035 authorize one minimum blocker-only correction of those
defects. It updates only validators, exact technical regressions, affected
source evidence, review history, and governing self-bindings. One immutable
subject receives the mandatory source and phase reviews. Its first complete
PASS checkpoints immediately and advances directly to P14 without optional
review or refinement.
