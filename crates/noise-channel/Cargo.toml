[package]
name = "noise-channel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "noise_channel"
path = "src/lib.rs"

[dependencies]
snow = { version = "=0.10.0", path = "../../vendor/snow-0.10.0", default-features = false, features = [
    "use-chacha20poly1305",
    "use-blake2",
    "use-curve25519",
] }

[lints.rust]
unsafe_code = "forbid"
