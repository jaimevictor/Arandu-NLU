# P11 Pre-phase Requirements Analysis

- Role: `independent-requirements-analysis`
- Analysis instance: `01a04b8a-aaa0-7640-bb12-e6fd766dd062`
- Input commit: `4cffe0e1711f55c28d0bd65a0737136f2270e183`
- Input tree: `b1ff17ba23d0f3960cdb7f586486dd7b6969a59e`
- Mode: read-only independent primary-evidence inspection
- Independence: no edits, network, siblings, internal/Amazon, or closed engine
- Result: `ANALYSIS_COMPLETE_BLOCKER_DISPOSITIONED`

## Mandatory Scope

P11 owns `GLB-INTENT-002` and `P11-MULTI-001..015`. The minimum result:

1. builds every node from checked clause-local predicate and argument evidence;
2. represents order, supported sharing, independence, and negation scope;
3. rejects contradiction, unsupported propagation, unresolved scope, stale
   catalog generations, and partial interpretations;
4. derives a graph execution class before later adapter routing;
5. emits stable graph structure and canonical bytes; and
6. measures exact graph outcomes on admitted train and development gold.

P11 consumes one complete P09 match and one immutable P10 catalog snapshot. It
does not absorb recognition, catalog synchronization, session state, risk
policy, authorization, execution, or response rendering.

Applicable inherited obligations include deterministic ordering, checked
half-open UTF-8 byte spans, bounded resources, privacy-safe diagnostics,
offline production behavior, source lineage, protocol-v1 compatibility, and
the four later action-admission prerequisites.

## Gold Boundary

P02 train and development records already cover single, parallel-pair, and
ordered-pair graphs. Its admitted contradiction and ambiguity suites cover
conflict and coordination attachment scope. P11 may mechanically project
those frozen labels, but it may not infer labels from recognizer or composer
output, inspect held-out records, or mutate P02 version 1.

The independent analysis found no admitted multi-intent negation stratum.
`P11-MULTI-014` was therefore a true source blocker on the input baseline.

The user subsequently authorized the executor to use the best bounded
workaround and to create the source when necessary. Under that newer explicit
direction and the existing `PROJECT_AUTHORED_SYNTHETIC` exception, P11 will
freeze a separate minimal Apache-2.0 supplement before any P11 composer code.
It is limited to node-local and ambiguous multi-intent negation scope, carries
pre-composer expected semantics, and supports internal conformance only. It is
not an external source, independent accuracy evidence, a P02 refreeze, or
permission to inspect held-out data.

## Minimum Acceptance

P11 is minimally acceptable only when:

- every `P11-MULTI-001..015` row and `GLB-INTENT-002` has reproducible
  evidence on one immutable candidate;
- the graph type rejects dangling, duplicate, cyclic, unsupported, mixed
  generation, contradictory, and over-limit structures;
- the composer never creates a node from a connective alone and never emits a
  subset after any clause fails;
- exact outcome and graph match reconcile by split and graph stratum;
- the project-authored negation supplement reproduces byte for byte and
  cannot be relabeled by NLU output;
- protocol-v1 fixtures remain byte-identical and v1 decoding remains
  supported;
- focused tests, strict clippy, locked build, source/license checks, privacy
  checks, and inherited gates pass;
- all mandatory reviewers pass the same frozen subject, no P0-P2 remains
  open, and any eligible P3 is deferred; and
- the evidence checkpoint validates.

No P11 accuracy threshold is specified. Metrics must report exact numerators,
denominators, source limitations, and abstentions without turning internal
conformance into an independent-language claim.

## Bounded Convergence

One convergence pass is one selected and dispositioned tuple of source/oracle
portfolio, semantic graph schema, P09/P10 composition route, evaluator
projection, and test strategy. This selected tuple is pass 1 of at most 3.
Candidate round 1 is the first complete minimum implementation; later rounds
are blocker-only. The first minimally acceptable candidate is checkpointed
without optional refinement.

## Counterexample

The admitted request `ligue a luz e o ventilador da sala` is an attachment
scope clarification. Splitting on `e`, copying `da sala` to both arguments,
or executing only one target would invent unsupported semantics. P11 must
clarify or abstain and return no plan.

## Deferred

P12 owns session continuation and referents. P13 owns policy, risk,
authorization, and operation admission. P14 owns live synchronization and
execution. P15 owns held-out aggregate release metrics. P16 owns the broader
adversarial false-plan and negation release gate.
