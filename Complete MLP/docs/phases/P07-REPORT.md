# P07 Lexical-evidence Morphology Report

- Phase: `P07`
- State: `COMPLETE`
- Subject: `37929728cc38761548d1693c6adb3a78bc518a2e`
- Subject tree: `737a42e85e3b570f49c68f5234fe0f138098bc54`
- Convergence pass: 1 of 3
- Candidate round: 1 of 3
- Result: `MINIMUM_ACCEPTABLE_PASS_WITH_USER_REVIEW_WAIVER`

## Delivered

P07 adds exact lexical-evidence morphology over the immutable P06 package and
an isolated deterministic evaluation crate. Runtime exposes payload-free
unknown, one complete lexical analysis, or every complete analysis for an
ambiguous surface. It adds no productive inference or new linguistic source.

The frozen internal-conformance view has explicit source, dataset, split,
case-inventory, artifact, runtime-package, grouping, metric, confusion,
error-taxonomy, and limitation identities. The tracked report is canonical,
integer-only, and free of paths, time, entropy, and locale-derived state.

## Validation

All P01 through P07 mandatory gates pass. Workspace formatting,
warnings-denied Clippy, 125 Rust tests, all-target builds, and 7 P07 validator
mutations pass. Two independent input roots emit the exact tracked report:
28/28 surface sets, 29/0/0 analysis counts, 1/1 preserved ambiguity, the
frozen confusion matrix, and an empty baseline error ledger.

The evaluation is explicitly same-source internal conformance. It does not
claim independent Portuguese accuracy, representativeness, generalization, or
equivalence to another product.

A no-hardlink, no-local clone reproduced the exact candidate and every
inherited and P07 gate offline with a copied admitted toolchain. The clone had
no object alternate and remained clean.

## Convergence

The first integrated runtime evidence type, evaluation boundary, frozen split,
metric/error contract, and test portfolio met minimum acceptance in
convergence pass 1. Candidate round 1 is frozen immediately. No optional
refinement follows; two convergence passes and two candidate rounds remain
unused.

## Review State

The user directed the executor to skip the final phase review after successful
mandatory gates. No separate post-candidate phase review is claimed.

## Next State

After exact-commit reproduction, the evidence-only closeout advances the queue
to P08 contextual POS without changing the P07 subject.
